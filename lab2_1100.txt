Задание требует заменить функция buble_sort  на быстрее функцию
так как, использую stable_sort,чтобы сортировать элементы в диапазоне [первый, последний) в порядке возрастания.
Порядок эквивалентных элементов гарантированно сохраняется.
Элементы сравниваются с использованием оператора <
bool operator < (team X)const{
        return solved>X.solved;
    }
    Предлогаю использовать знак >, чтобы не поменять место когда 2 число равным, так как гарантированно сохраняется.
